<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/common_margin_left"
    android:orientation="vertical"
    app:cardBackgroundColor="@color/color_library_bg"
    app:cardCornerRadius="@dimen/common_radius"
    app:cardElevation="@dimen/common_radius">

    <LinearLayout
        android:id="@+id/item_library_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/item_library_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/app_name"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/item_library_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/common_radius"
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/font_normal" />

    </LinearLayout>

</android.support.v7.widget.CardView>